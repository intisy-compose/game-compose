# Copy to config.env and fill in. config.env is gitignored (holds secrets).

# Tunnel provider that exposes the servers publicly: playit | frp
TUNNEL=playit

# --- playit.gg (used when TUNNEL=playit) ---
# Secret from your playit.gg account/agent.
PLAYIT_SECRET=

# --- frp (used when TUNNEL=frp) ---
# The launcher auto-provisions frps on this VPS over SSH if it isn't running.
FRP_SERVER_ADDR=
FRP_SERVER_PORT=7000
FRP_TOKEN=
# SSH used to set up frps on the VPS (key path relative to this folder is fine).
FRP_SSH_USER=ubuntu
FRP_SSH_KEY=vps/ssh-key
